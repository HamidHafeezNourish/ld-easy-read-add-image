import { PaginatorComponentToken } from '@/themes/primePresetTypes'

export const pulsePaginatorTheme: PaginatorComponentToken = {
  navButton: {
    selectedBackground: 'var(--pulse-color-primary-10)',
    selectedColor: 'var(--pulse-color-primary-100)',
  },
}
