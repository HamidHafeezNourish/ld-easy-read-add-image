import { TreeComponentToken } from '@/themes/primePresetTypes'

export const pulseTreeTheme: TreeComponentToken = {
  node: {
    hoverBackground: 'var(--pulse-color-primary-10)',
    hoverColor: 'var(--pulse-color-primary-120)',
    selectedBackground: 'var(--pulse-color-primary-20)',
    selectedColor: 'var(--pulse-color-neutral-120)',
  },
}
