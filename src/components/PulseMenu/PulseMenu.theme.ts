import { MenuComponentToken } from '@/themes/primePresetTypes'

export const pulseMenuTheme: MenuComponentToken = {
  css: () => `
    .p-menu-item-label {
        font-family: 'Inter';
        font-weight: 400;
        font-size: 14px;
    }`,
}
